openapi: 3.0.0
info:
  title: workita API
  version: 1.0.0
  description: API specification for workita AI workshop project

servers:
  - url: http://localhost:3010
    description: Local development

paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: Service is running
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
